#include <bits/stdc++.h>
using namespace std;

void primeN(long long int n)
{

  for (long long int i = 2; i < n; i++)
  {
    if (n % i == 0)
  }
}

int main()
{
  //code
  int t;
  long long int n;
  vector<long long int> primeArray;
  cin >> t;
  while (t--)
  {
    cin >> n;
  }
  return 0;
}