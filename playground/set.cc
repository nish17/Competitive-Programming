#include <bits/stdc++.h>
using namespace std;

int main()
{

  string s;

  // set<int> s;
  // for (int i = 0; i < 10; i++)
  //   s.insert(rand() % 10 + 1);

  // set<int>::iterator it;

  // for ()
  // {
  // }

  // return 0;
}