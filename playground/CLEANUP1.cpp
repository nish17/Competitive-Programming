#include <bits/stdc++.h>
using namespace std;
int main()
{
  int t, n, m;
  cin >> t;
  while (t--)
  {
    cin >> n >> m;
    int _array1[m], _array2[n];
    bool _array3[n];
    for (int i = 0; i < m; i++)
      cin >> _array1[i];

    sort(_array1, _array1 + m);

    for (int i = 1; i <= n; i++)
    {
      if (_array1[i - 1])
    }

    cout << endl;
    return 0;
  }