#include <bits/stdc++.h>
#include <vector>
using namespace std;

void binarySearch(vector<int> array, int target)
{
	int low = 0;
	int high = array.size();
	int mid = (low + high) / 2;
	while (low <= high)
	{
	}
}

int main()
{
	vector<int> t;
	t.push_back(1);
	t.push_back(3);
	t.push_back(5);
	t.push_back(6);
	t.push_back(7);
	binarySearch(t, 2);
}
