#include <bits/stdc++.h>
using namespace std;

long long int rev(long long int x)
{
  long long int temp, rem;
  temp = x;
  while (temp >= 0)
  {
    rem = temp % 10;
  }
}

int main()
{
  int t;
  long long int n;
  cin >> t;
  while (t--)
  {
    cin >> n;
  }
  return 0;
}